# UI Analyzer 开发进度

## 已完成

- [x] `config.json` 配置 DeepSeek API key，后端自动调用
- [x] 前端移除 API key 填写入口，改为服务端统一管理
- [x] 标注图叠加虚线容器框（已回退）
- [x] 区域列表：按交互功能类型分组（可点击 / 可输入 / 可阅读...）

## 回退

- 容器重叠检测 + 容器分组展示功能已完全移除（概念太抽象，效果不佳）

## 部署（阿里云轻量应用服务器）

- **服务器 IP**: 8.133.177.204
- **访问地址**: http://8.133.177.204:8081/（需配置防火墙开放 8081 端口）
- **Nginx 反向代理**: 8080 → 8081，带 HTTP Basic Auth 密码保护
- **Systemd 服务**: `ui-analyzer.service`，开机自启
- **同服务器其他项目**: 元规助手（端口 80），两者互不影响

## 代码安全

- 前端静态文件通过 Nginx 反向代理保护，需密码访问
- API Key 存储在服务端 `config.json`，不暴露给前端
- config.json、SAM 模型文件、uploads/output 目录已加入 .gitignore
- GitHub 仓库：`elowenliu0305/ui-analyzer`（public，不含敏感文件）

## 当前状态

- http://localhost:8080 运行中
- 上传截图 → OpenCV/SAM 检测 → 线检测 → 区域精炼 → 段落分配 → LLM 理解

### 线检测（`line_detector.py`）

三种分隔线识别：
| 类型 | 颜色 | 方法 |
|------|------|------|
| 实体线（红色） | 锐利 CSS border | Canny + 形态学检测 |
| 界面线/纯色·混色（橙色） | 纯色与会渐变区域交界 | 颜色过渡滑动窗口分析 |
| 界面线/混色·混色（黄色） | 两个纹理区域之间 | 颜色统计过渡分析 |
| 竖线（实体线） | Sobel X 列投影 | Sobel 梯度 + 列峰值检测 |

当前支持横向 + 竖向线检测，33 条线/图（横 19 + 竖 14）。

### 区域精炼（`region_refiner.py`）

- **线切割**: 用实体线切分跨区域的过大区域（递归，MIN_PIECE_SIZE=25）
- **区域合并**: 合并相邻同类型区域（>50% x 重叠，gap < 2× 高度，中间无分隔线）
- **段落分层**: `assign_sections()` 用水平实体线划分段落，每个 region 获得 section 信息
- **后过滤**: 丢弃 tiny unknown 区域
- **切割可视化**: 蓝色=切前，绿色=切后，红色=实体线

### 前端

- 3 视图切换：区域标注 / 段落色带 / 分隔线可视化
- 区域列表两级分组：段落 → 交互功能（可点击/可输入/导航/阅读...）
- 图例显示区域类型 + 分隔线统计详情

### 管道流程

```
detect_regions → detect_lines → refine_regions_with_lines → assign_sections → analyze_regions_with_llm
```

### 测试结果

- `1.png`（桌面测试图）：136 个区域，33 条线（横19+竖14），15 个段落
- 图片 3（多列布局测试）：200 个区域，27 条线（横20+竖7），8 个段落

## 待办

- [ ] 阿里云防火墙添加 8081 端口规则后，外网方可访问
- [ ] LLM 理解接入后验证段落分组效果
- [ ] 竖线检测准确率仍需优化（漏检/误检）
